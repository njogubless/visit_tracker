class Activity {
  final int id;
  final String description;
  final DateTime createdAt;
  
  const Activity({
    required this.id,
    required this.description,
    required this.createdAt,
  });
}